"0","-- Used LEFT JOIN to preserve original rows in the Incident table
-- and grouped by airline id to determine number of incidents and average delay
SELECT 
  a.name AS Airline,
  COUNT(i.inId) AS Num_Incidents,
  ROUND(AVG(f.delay), 2) AS Avg_Delay_mins
FROM Incident i
LEFT JOIN Flight f
  ON i.fId = f.fId
LEFT JOIN Airline a
  ON f.alId = a.alId
GROUP BY a.alId
ORDER BY a.alId"
